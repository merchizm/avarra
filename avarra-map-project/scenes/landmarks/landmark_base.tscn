[gd_scene load_steps=4 format=3 uid="UID://landmark_base"]

[ext_resource type="Script" path="res://scripts/landmark_manager.gd" id=1]

[node name="LandmarkBase" type="Node3D"]
script = ExtResource( 1 )

[node name="ModelRoot" type="Node3D"]
    # This will hold the actual 3D model (GLB) that gets instanced at runtime

[node name="SelectionArea" type="Area3D"]
    monitorable = true
    monitor_pickable = true
    [subresource type="SphereShape3D"]
        radius = 5.0  # Adjust based on landmark size

[node name="LabelAnchor" type="Node3D"]
    transform = Transform3D( Basis(), Vector3(0, 10, 0) )  # Above the landmark

[node name="Particles" type="GPUParticles3D"]
    # Placeholder for particle effects (magic, smoke, etc.)

[node name="AudioPlayer" type="AudioStreamPlayer3D"]
    # Placeholder for ambient sounds
